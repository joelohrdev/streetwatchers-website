/** Button styles shared by the public site's forms. */
export const primaryButtonClass =
    'font-display border-ink bg-ink text-paper hover:bg-paper hover:text-ink inline-flex w-full items-center justify-center gap-2 border px-8 py-4 text-xs font-semibold tracking-[0.14em] uppercase transition-colors disabled:opacity-50';

export const secondaryButtonClass =
    'font-display border-ink/25 text-ink hover:border-ink inline-flex w-full items-center justify-center gap-2 border px-8 py-4 text-xs font-semibold tracking-[0.14em] uppercase transition-colors disabled:opacity-50';

/** An inline text link, underlined in ink. */
export const inlineLinkClass = 'text-ink border-ink border-b';
