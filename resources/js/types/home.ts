export type FeaturedPhoto = {
    id: number;
    caption: string;
    credit: string;
    location: string;
};

export type ChapterDirectoryStats = {
    chapterCount: number;
    countryCount: number;
};
